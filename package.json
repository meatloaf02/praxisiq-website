{
  "name": "praxisiq-site",
  "version": "2.0.0",
  "type": "module",
  "description": "PraxisIQ consulting website — built with Eleventy",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
